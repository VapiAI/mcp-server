export async function getChangelog(
  version?: string,
  limit?: number,
  type?: string,
): Promise<string> {
  // Placeholder - changelog fetching logic
  return `## Changelog\n\nVisit https://docs.vapi.ai/changelog for the latest changes.`;
}
